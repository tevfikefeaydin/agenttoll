# Website and operations verification

Owner: root. Scope: audited onboarding/docs defects, Basename diagnostic and cancellation omission, actual data coverage/freshness monitoring. No new payment or wallet signature in these checks.

- Hidden CSS now overrides author display styles. Fresh desktop 1280px and mobile390px pages report `hidden:true`, computed display `none`, width0 for `#demo-pay`. A real unsigned local Base Sepolia quote returns402/$0.004 and only then reveals the button. Configured demo receiver is the nonfunded public1111 address; no wallet connection or payment occurred.
- Browser checks ran with Playwright CLI after the connected-browser tool reported no browser. Measured document widths exactly matched1280 and390; no horizontal overflow. The sole browser console error was the expected network402 quote. Desktop and mobile quote screenshots are at `output/playwright/remediation/`; production verification is separate.
- Basename regression first failed: four actual RPCs logged as zero; caller cancellation left the RPC alive. After the change20 Basename/site tests passed. Counts now reflect actual RPC attempts, cache hits add none, caller abort cancels the underlying request before fallback.
- The independent data worker found two monitor-validation gaps (summary/row inconsistencies). Both were reproduced and fixed; all five data monitor tests pass. The monitor reports coverage separately from availability, refuses stale/future snapshot timestamps, bounds slow providers and omits raw errors.
- The built data monitor was executed against real public upstreams at2026-09-14T16:32:52Z: `ok:true,degraded:true`; snapshot captured14:16:59Z, age8152s, actual capture lag24839s against07:23UTC; scorecard11priced/15low-observed-liquidity/0unavailable from26tokens; safety5/8checks. See `live-data-monitor.json`. These are observation counts, not a retention or profitability claim.
- Host monitor has three offline subprocess/state validation tests and was independently reviewed. Root owns installing it after the source release and verifying the active timer/latest report; preparing the files alone is not activation.
- Privacy, terms, setup, Security and Operations docs now reflect the real log fields, Hetzner, size-based3x10MB Docker rotation, x402v2 migration, finite MCP controls, partial data and ambiguous-payment retry limits. Actual rotation configuration was read from the active container; no broad legal-compliance claim is made.

The existing daily paying jobs are unchanged. The additional hourly monitor performs unsigned/direct read-only checks. Historical missing client events cannot be reconstructed, and upgrading our package cannot restart a user's already-running pinned old client.
